<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/scrollProfileEdit"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:fillViewport="true"
    android:background="@android:color/transparent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="12dp"
        android:gravity="center_horizontal"
        android:clipToPadding="false"
        android:background="#88000000"> <!-- nền mờ đen phía sau -->

        <!-- Tiêu đề -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/profile_title"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="#FFFFFF"
            android:layout_marginBottom="4dp"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/profile_subtitle"
            android:textSize="14sp"
            android:textColor="#FFCCFF"
            android:layout_marginBottom="16dp"/>

        <!-- Email -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/email_label"
            android:textStyle="bold"
            android:textColor="#FFFFFF"
            android:layout_marginBottom="4dp"/>

        <EditText
            android:id="@+id/edtEmail"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:hint="@string/email_hint"
            android:background="@drawable/edit_text_bg"
            android:textColor="#88FFFFFF"
            android:textColorHint="#CCDDFF"
            android:padding="12dp"
            android:enabled="false"
            android:inputType="textEmailAddress" />

        <!-- Username -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/username_label"
            android:textStyle="bold"
            android:textColor="#FFFFFF"
            android:layout_marginTop="12dp"
            android:layout_marginBottom="4dp"/>

        <EditText
            android:id="@+id/edtUsername"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:hint="@string/username_hint"
            android:background="@drawable/edit_text_bg"
            android:textColor="#FFFFFF"
            android:textColorHint="#CCDDFF"
            android:padding="12dp"
            android:inputType="text"/>

        <TextView
            android:id="@+id/txtUsernameInfo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="#FFCC99"
            android:textSize="12sp"
            android:layout_marginBottom="12dp"
            android:text="@string/username_info"/>

        <!-- Display Name -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/displayname_label"
            android:textStyle="bold"
            android:textColor="#FFFFFF"
            android:layout_marginBottom="4dp"/>

        <EditText
            android:id="@+id/edtDisplayName"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:hint="@string/displayname_hint"
            android:background="@drawable/edit_text_bg"
            android:textColor="#FFFFFF"
            android:textColorHint="#CCDDFF"
            android:padding="12dp"
            android:inputType="text"/>

        <!-- Bio -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/bio_label"
            android:textStyle="bold"
            android:textColor="#FFFFFF"
            android:layout_marginBottom="4dp"/>

        <EditText
            android:id="@+id/edtBio"
            android:layout_width="match_parent"
            android:layout_height="120dp"
            android:hint="@string/bio_hint"
            android:background="@drawable/edit_text_bg"
            android:textColor="#FFFFFF"
            android:textColorHint="#CCDDFF"
            android:padding="12dp"
            android:gravity="top"
            android:inputType="textMultiLine"/>

        <!-- Save Button -->
        <Button
            android:id="@+id/btnSaveProfile"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:text="@string/save_button"
            android:backgroundTint="#FF99FF"
            android:textColor="#FFFFFF"
            android:layout_marginTop="16dp"/>
    </LinearLayout>
</ScrollView>